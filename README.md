# 🧾 สมุดเก็บรายจ่ายของฉัน

เว็บแอปหน้าเดียวสำหรับเก็บรายจ่าย: **อัปโหลดรูปใบเสร็จ → อ่านข้อความด้วย OCR (ไทย + อังกฤษ) →
ดึงวันที่ / ชื่อร้าน / ยอดเงิน / หมวดออกมาให้อัตโนมัติ → บันทึกเป็นสรุปรายจ่ายรายเดือน**

ทุกอย่างทำงานในเบราว์เซอร์ของคุณเอง ไม่มีเซิร์ฟเวอร์ ไม่มีการอัปโหลดรูปหรือข้อความออกไปที่ไหน
ข้อมูลถูกเก็บใน `localStorage` ของเครื่องคุณ

## หน้าตาแอป (v45)

- มือถือ: **เมนูหลักอยู่ล่างจอ** (เพิ่ม · สรุป · รายการ · ลูกหนี้ · เพื่อน) เป็นไอคอน+ป้ายตัวเลข กดถึงด้วยนิ้วโป้ง · เดสก์ท็อป: แท็บบนตามเดิม
- ปุ่มหลักเป็น **การ์ดไอคอน** (เลือกรูป / ถ่ายรูป / กรอกเอง · CSV / สำรอง / กู้คืน / ล้าง) ตัดข้อความอธิบายยาวๆ ออก เหลือเฉพาะที่จำเป็น

## แท็บ “ฉัน” (v55 · ปรับใน v56)

การ์ดทางเข้า 2 ใบ + สรุปข้อมูลของตัวเอง (ชื่อเล่น · พร้อมเพย์ · บัญชีที่ล็อกอิน)

- **💰 QR รับเงิน** — สร้าง QR พร้อมเพย์ของตัวเองให้คนอื่นสแกนโอนให้ ใส่จำนวนเงินหรือเว้นว่างให้ผู้โอนกรอกเองก็ได้
  · บันทึก/แชร์เป็นรูป PNG · คัดลอกเลขพร้อมเพย์
- **🪪 บัตรของฉัน** — QR ให้เพื่อนสแกนเพิ่มเราเป็นเพื่อน (เข้าได้จากแท็บเพื่อนเหมือนเดิม)
- เมนูลัดที่ไอคอนแอป: `#go=add` · `#go=debt` · `#go=scan` · `#go=myqr`
- v56 เอาการ์ด “สแกนจ่ายเงิน” ออกตามที่ผู้ใช้ขอ (ตัวอ่าน QR `PromptPay.parse` ยังอยู่ใน `assets/promptpay.js` เผื่อใช้ภายหลัง)

## ทำเป็นแอป iOS (TestFlight) โดยไม่ต้องมี Mac

iOS ไม่มี TWA จึงห่อเว็บด้วย `WKWebView` เป็นแอป native แล้วบิลด์บน macOS runner ของ GitHub Actions

- โปรเจกต์อยู่ที่ [`ios/`](ios/) · วิธีตั้งค่าและข้อจำกัดทั้งหมดอยู่ใน **[ios/README.md](ios/README.md)**
- ต้องมี Apple Developer Program ($99/ปี) แต่**ไม่ต้องมี Mac**
- กดรันเวิร์กโฟลว์ **iOS → TestFlight** ในแท็บ Actions แล้วบิลด์จะขึ้น TestFlight เอง
- แอปเปิดเว็บตัวเดียวกัน → **อัปเดตเว็บแล้วแอปได้ของใหม่ทันที** ส่งบิลด์ใหม่เฉพาะตอนเปลี่ยนไอคอน/ชื่อ/สิทธิ์/โค้ด Swift
- ข้อจำกัดสำคัญ: **Web Push ใช้ไม่ได้ใน WKWebView** (แจ้งเตือนในแอปยังทำงาน แต่ตอนปิดแอปจะไม่เด้ง)

## ทำเป็นแอป Android (.apk / .aab) ด้วย TWA (v59)

TWA (Trusted Web Activity) คือแอป Android ที่เปิดเว็บนี้ด้วยเครื่องยนต์ Chrome แบบ **ไม่มีแถบ URL**
โค้ดชุดเดียวกับเว็บ อัปเดตเว็บเมื่อไหร่แอปในเครื่องผู้ใช้ก็ได้ของใหม่ทันที ไม่ต้องส่งรีวิวใหม่

### ในรีโปเตรียมไว้ให้แล้ว

| ไฟล์ | ใช้ทำอะไร |
| --- | --- |
| `.well-known/assetlinks.json` | ผูกโดเมนกับแอป — **ต้องแก้ลายนิ้วมือก่อน** ไม่งั้นแอปจะโผล่แถบ URL ด้านบน |
| `manifest.webmanifest` | เพิ่ม `id`, `categories`, `screenshots`, `prefer_related_applications` ครบตามที่ PWABuilder/Play ต้องการ |
| `assets/screenshots/*.png` | ภาพหน้าจอ 4 มือถือ (824×1464) + 2 จอกว้าง (1280×800) — อัตราส่วนผ่านเกณฑ์ Play ใช้ยื่นในสโตร์ได้เลย |
| `privacy.html` | หน้านโยบายความเป็นส่วนตัว (Play บังคับต้องมี URL) — **ต้องใส่ชื่อกับอีเมลผู้ดูแลก่อน** |

### ขั้นตอน

**1. สร้างแพ็กเกจ**

เปิด <https://www.pwabuilder.com> → ใส่ `https://ampaypay.onrender.com` → *Package For Stores* → **Android**

ตั้งค่าให้ตรงกับที่เตรียมไว้:

| ช่อง | ค่า |
| --- | --- |
| Package ID | `com.onrender.ampaypay.twa` (ค่าเริ่มต้นที่ PWABuilder ตั้งจากชื่อโดเมน — ต้องตรงกับใน `assetlinks.json` เป๊ะ ๆ) |
| App name | `AmPayPay` |
| Launcher name | `AmPayPay` |
| App version / version code | `1.0.0` / `1` |
| Display mode | `standalone` |
| Signing key | *Create new* (ครั้งแรก) แล้ว **เก็บไฟล์ `signing.keystore` กับรหัสผ่านไว้ให้ดี** — หายแล้วอัปเดตแอปเดิมบน Play ไม่ได้อีกเลย |

ได้ไฟล์ zip ที่มี `app-release-signed.apk` (ติดตั้งลองเองได้), `app-release-bundle.aab` (ไฟล์ที่ส่งขึ้น Play), คีย์ และ `assetlinks.json`

**2. ผูกโดเมนกับแอป**

เปิด `assetlinks.json` ที่ได้จาก zip → คัดลอกค่าใน `sha256_cert_fingerprints`
(รูปแบบ `AA:BB:CC:...` 32 คู่) มาแทนที่ `REPLACE_WITH_SHA256_FINGERPRINT_FROM_PWABUILDER`
ในไฟล์ `.well-known/assetlinks.json` ของรีโปนี้ แล้ว commit + push ขึ้น main

ตรวจว่าขึ้นจริง:

```bash
curl -s https://ampaypay.onrender.com/.well-known/assetlinks.json
```

ถ้าอยากให้ Google ตรวจให้: <https://developers.google.com/digital-asset-links/tools/generator>
หรือเช็คตรง ๆ ว่า Google อ่านไฟล์เราเห็นเป็นอะไร:

```bash
curl -s "https://digitalassetlinks.googleapis.com/v1/statements:list\
?source.web.site=https://ampaypay.onrender.com\
&relation=delegate_permission/common.handle_all_urls"
```

**ยังขึ้นแถบ URL อยู่ ตรวจตามนี้ทีละข้อ** (สามข้อนี้คือสาเหตุเกือบทั้งหมด)

1. **ชื่อแพ็กเกจไม่ตรง** — เปิดไฟล์ `assetlinks.json` ที่อยู่ในซิปของ PWABuilder ดูค่า `package_name`
   ต้องมีชื่อนั้นอยู่ใน `.well-known/assetlinks.json` ของเว็บด้วย (ไฟล์นี้ใส่ได้หลายชื่อ ถ้าต้องรองรับหลายเวอร์ชัน)
2. **ลายนิ้วมือคนละคีย์** — คีย์ที่เซ็น `.apk` ต้องเป็นคีย์เดียวกับที่ประกาศไว้ ดูของจริงจากไฟล์ที่ติดตั้ง:
   ```bash
   keytool -printcert -jarfile app-release-signed.apk | grep SHA256
   ```
3. **Android จำผลตรวจเก่าไว้** — ถ้าติดตั้ง `.apk` *ก่อน* ที่ไฟล์จะขึ้นเว็บ ต้อง **ถอนแล้วติดตั้งใหม่**
   (ล้างข้อมูลแอป Chrome ด้วยยิ่งดี) เพราะผลตรวจถูกแคชไว้ตั้งแต่ตอนติดตั้ง

> **สำคัญตอนขึ้น Play:** ถ้าเปิด Play App Signing (ค่าเริ่มต้น) Google จะ **เซ็นแอปใหม่ด้วยคีย์ของ Play**
> ทำให้เวอร์ชันที่โหลดจากสโตร์มีลายนิ้วมือคนละอันกับ `.apk` ที่ทดสอบเอง
> ต้องเข้า Play Console → *Setup → App signing* คัดลอก **SHA-256 ของ App signing key**
> มาเพิ่มเป็นอีกค่าใน `sha256_cert_fingerprints` (ใส่ได้หลายค่าในอาร์เรย์เดียวกัน) ไม่งั้นแอปจากสโตร์จะโชว์แถบ URL

**3. ทดสอบก่อนขึ้นสโตร์**

ส่งไฟล์ `.apk` เข้าเครื่อง Android แล้วติดตั้ง (เปิด “อนุญาตติดตั้งจากแหล่งนี้”) เปิดแอปแล้วดู:

- ✅ ไม่มีแถบที่อยู่เว็บด้านบน = `assetlinks.json` ถูกต้องแล้ว
- ❌ ถ้ายังมีแถบ URL → ลายนิ้วมือไม่ตรง / ไฟล์ยังไม่ deploy / package id ไม่ตรง
  (แก้แล้วให้ลบข้อมูลแอป Chrome หรือถอนแล้วติดตั้งใหม่ เพราะ Android แคชผลตรวจไว้)

**4. ขึ้น Google Play**

- สมัคร Play Console **$25 จ่ายครั้งเดียว** <https://play.google.com/console>
- สร้างแอป → อัปโหลด `.aab` → กรอกข้อมูลร้านค้า
- ภาพที่ต้องใช้มีครบในรีโปแล้ว: ภาพหน้าจอ `assets/screenshots/` (ต้องมีอย่างน้อย 2 ภาพ) · ไอคอน 512×512 `assets/icon-512.png` · **Feature graphic 1024×500** `assets/store/feature-graphic.png` (แบบพื้นครีมอ่อนอยู่ที่ `feature-graphic-light.png` เลือกใช้อันไหนก็ได้)
- Privacy policy URL: `https://ampaypay.onrender.com/privacy.html`
- แบบฟอร์ม **Data safety** ตอบตามนี้ได้

  | หัวข้อ | คำตอบ |
  | --- | --- |
  | เก็บข้อมูลไหม | เก็บ (เฉพาะเมื่อผู้ใช้เปิดซิงก์) |
  | ประเภทข้อมูล | อีเมล (Personal info) · ข้อมูลการเงินที่ผู้ใช้กรอกเอง (App activity/User content) · รูปภาพที่ผู้ใช้เลือก |
  | ส่งออกนอกเครื่องไหม | ส่งเมื่อเปิดซิงก์เท่านั้น เข้ารหัสระหว่างส่ง (HTTPS) |
  | แชร์กับบุคคลที่สามไหม | ไม่ |
  | ลบข้อมูลได้ไหม | ได้ (ปุ่มล้างข้อมูลในแอป + ขอลบบัญชีทางอีเมล) |
  | ใช้เพื่อโฆษณา/ติดตามผู้ใช้ไหม | ไม่ |

- ต้องผ่าน target API level ล่าสุดที่ Play กำหนด — PWABuilder ตั้งให้อยู่แล้ว ถ้า Play เตือนให้กด *Generate* ใหม่จาก PWABuilder แล้วอัปโหลดทับ (ใช้คีย์เดิม)

### แก้ Feature graphic เอง

ต้นฉบับเป็น HTML อยู่ที่ `tools/feature-graphic.html` (เขียว) และ `tools/feature-graphic-light.html` (ครีม)
แก้ข้อความ/สีในไฟล์แล้วเรนเดอร์ใหม่ด้วย Playwright:

```bash
python3 -m http.server 8125 &                     # เสิร์ฟจากรากรีโป
node -e "const {chromium}=require('playwright');(async()=>{const b=await chromium.launch();
const p=await (await b.newContext({viewport:{width:1024,height:500},deviceScaleFactor:1})).newPage();
await p.goto('http://localhost:8125/tools/feature-graphic.html');await p.waitForTimeout(700);
await p.screenshot({path:'assets/store/feature-graphic.png'});await b.close();})()"
```

ได้ PNG ขนาด 1024×500 แบบ 24-bit ไม่มี alpha ตรงตามที่ Play ต้องการพอดี

### อัปเดตแอปหลังจากนี้

- แก้เว็บแล้ว push ขึ้น main → **แอปได้ของใหม่เอง** ไม่ต้องทำอะไรกับ Play
- ส่งเวอร์ชันใหม่ขึ้น Play เฉพาะตอนเปลี่ยน **ไอคอน ชื่อแอป package id หรือ target API** เท่านั้น (สร้างใหม่จาก PWABuilder ด้วยคีย์เดิม + เพิ่ม version code)

### ทางเลือกที่ไม่ต้องขึ้นสโตร์

ส่งไฟล์ `.apk` ให้กันตรง ๆ ก็ติดตั้งได้ หรือให้เปิดเว็บใน Chrome แล้วกดเมนู ⋮ → **ติดตั้งแอป** ก็ได้ไอคอน เปิดเต็มจอ ใช้ออฟไลน์และรับแจ้งเตือนได้เหมือนกัน

## กันคลุมข้อความและก็อบปี้หน้าจอ (v65)

- `body` ตั้ง `user-select: none` + `-webkit-touch-callout: none` — ลากคลุมไม่ได้ แตะค้างบน iOS ไม่เด้งเมนูคัดลอก
- ปิดอีเวนต์ `copy` · `cut` · `contextmenu` · `dragstart` · `selectstart` ทั้งหน้า
- **ยกเว้นช่องกรอกข้อมูล** (`input` / `textarea` / contenteditable) ยังพิมพ์ เลือก คัดลอก วางได้ครบเหมือนเดิม
- ปุ่ม "คัดลอก" ในแอปใช้ Clipboard API จึงไม่โดนกฎนี้
- ลากรูปออกจากหน้าจอไม่ได้ (`-webkit-user-drag: none`)

> กันได้แค่การหยิบข้อความออกแบบง่าย ๆ — **ถ่ายภาพหน้าจอยังทำได้เสมอ** และโค้ดหน้าเว็บยังเปิดดูได้จาก devtools ตามปกติของเว็บทุกเว็บ

## แจ้งเตือนตอนปิดแอปกับการล็อกอินหลายเครื่อง (v64)

ตารางบนเซิร์ฟเวอร์ใช้ `endpoint` เป็น primary key = **หนึ่งแถวต่อหนึ่งเบราว์เซอร์** และ Edge Function ส่งวนทุกแถวที่ตรงกับอีเมล/บัญชีนั้น
โครงสร้างจึงรองรับหลายเครื่องอยู่แล้ว แต่ฝั่งแอปมีสามช่องโหว่ที่ทำให้บางเครื่องเงียบ — แก้แล้วใน v64

| กรณี | เดิม | ตอนนี้ |
| --- | --- | --- |
| เคยอนุญาตแจ้งเตือนไว้ แต่ข้อมูลในเครื่องถูกล้าง | ไม่ลงทะเบียนให้อีกเลย (`if (!had) return`) | เปิดแอปแล้วลงทะเบียนให้เอง |
| สลับบัญชีบนเครื่องเดิม | แถวยังผูกกับเจ้าของเดิม — เจ้าของใหม่ไม่ได้รับ ส่วนเจ้าของเดิมยังได้ทั้งที่ไม่ได้ใช้เครื่องนั้นแล้ว | เขียนทับเป็นบัญชีที่ล็อกอินอยู่ทันที |
| ล็อกอินระหว่างใช้งาน (ไม่ได้รีโหลด) | ต้องรีโหลดก่อนถึงลงทะเบียน | ลงทะเบียนให้ทันทีผ่าน `CloudSync.onState` |

- รอยจำในเครื่อง (`expense-book:push:v1`) เก็บอีเมลของบัญชีที่ลงทะเบียนไว้ด้วย จะได้รู้ว่าต้องเขียนทับเมื่อสลับบัญชี
- ปุ่ม **🩺 ตรวจการตั้งค่า** บอกจำนวนเครื่องที่จะได้รับแจ้งเตือนพร้อมชนิดเครื่อง เช่น *“2 เครื่อง — Android · Chrome · iPhone/iPad · Safari”*

## สีกราฟบอกความหนักของรายจ่าย (v62 · ปรับช่วงสีใน v63)

แท่งกราฟทุกอันเปลี่ยนสีตามสัดส่วนเทียบกับแท่งที่สูงที่สุดในกราฟนั้น

| สัดส่วน | สี |
| --- | --- |
| ต่ำกว่า 80% | 🟢 เขียว (`--heat-1`) |
| 80–89% | 🟡 เหลือง (`--heat-2`) |
| 90–99% | 🟠 ส้ม (`--heat-3`) |
| 100% | 🔴 แดง (`--heat-4`) |

- ใช้กับกราฟ **รายจ่ายรายวัน · แยกตามหมวด · แนวโน้ม 6 เดือน**
- หมวดที่ **เกินงบ** ยังเป็นสีแดงเสมอ ไม่ว่าแท่งจะสั้นแค่ไหน
- วันนี้/เดือนปัจจุบันเปลี่ยนจาก "ทับสีม่วง" เป็น **วงแหวน** จะได้ยังเห็นสีตามสัดส่วน
- มีคำอธิบายสี (ต่ำกว่า 80% · 80–89% · 90–99% · 100%) ใต้กราฟรายวันและกราฟหมวด
- ไอคอนแท็บ "สรุป" เปลี่ยนจาก 📊 เป็น **กราฟวงกลม** (inline SVG คมทุกขนาดจอ)

## เมนูล่างหลุดจากขอบจอบน iPhone (v58)

พิมพ์ในช่องกรอกแล้วปิดแป้นพิมพ์ แถบเมนูล่าง (`position: fixed`) ค้างลอยอยู่กลางจอทับเนื้อหา — เป็นอาการของ Safari บน iOS ที่ไม่ย้ายอิลิเมนต์แบบ fixed กลับที่เดิมหลังแป้นพิมพ์ยุบ

- ฟังก์ชัน `visualViewport` จับตอนแป้นพิมพ์เด้งขึ้น → เก็บแถบลงไป (`.tabbar.is-away`) แล้วสะกิดให้ Safari วาดใหม่ตอนปิด (ตอนซูมนิ้วไม่ยุ่งด้วย)
- v60: เก็บแถบเฉพาะตอน**กำลังพิมพ์อยู่จริง** (มีช่องกรอกที่โฟกัสอยู่) ไม่งั้นเบราว์เซอร์ที่มีแถบเครื่องมือของตัวเอง เช่น Chrome Custom Tab หรือ in-app browser จะโดนซ่อนเมนูทั้งที่ไม่มีแป้นพิมพ์
- แถบเมนูเป็นเลเยอร์ของตัวเอง (`transform: translateZ(0)`) กัน Safari วาดภาพค้างทับ
- ย้ายแสงพื้นหลังจาก `background-attachment: fixed` (ตัวที่ทำให้ Safari วาดพลาด) ไปไว้บนชั้น `position: fixed` ของตัวเอง
- เติม `-webkit-backdrop-filter` ให้ครบทุกจุดที่เบลอพื้นหลัง — iOS ยังต้องใช้คำนำหน้านี้

## สถานะใบแจ้งหนี้ให้ตรงกันทั้งสองเครื่อง (v57)

เดิมฝั่งเจ้าหนี้กด **“รับเงินแล้ว”** แล้วตัดยอดเฉพาะในเครื่องตัวเอง ใบแจ้งหนี้บนเซิร์ฟเวอร์ยังเป็น `paid`
เครื่องลูกหนี้จึงค้างอยู่ที่ “แจ้งว่าจ่ายแล้ว” ตลอด (มีแค่ปุ่ม “ยืนยันได้รับ” ที่ส่งสถานะให้จริง และปุ่มนั้นก็หายไปหลังตัดยอด)

- กด “รับเงินแล้ว” หรือ “รับครบแล้ว” → ส่งสถานะ `confirmed` ให้เพื่อนด้วย ลูกหนี้เห็น **“ได้รับเงินแล้ว”** ทันทีที่ซิงก์
- กด “ยกเลิก” → ย้อนกลับเป็น `paid` (ถ้าลูกหนี้เคยแจ้งไว้) หรือ `pending`
- ปุ่ม “ยืนยันได้รับ” ยังขึ้นให้กดได้ แม้ตัดยอดในเครื่องไปแล้ว ตราบใดที่เพื่อนยังไม่เห็นสถานะล่าสุด
- รายการเก่าที่ค้างอยู่ ระบบยืนยันย้อนหลังให้อัตโนมัติตอนซิงก์ (ยิงครั้งเดียวต่อใบ ไม่วนซ้ำ)

## ความปลอดภัย (ตรวจและแก้ในเวอร์ชัน 53)

ตรวจทั้งฝั่งหน้าเว็บ ฐานข้อมูล และ Edge Function แล้วแก้จุดที่เสี่ยงดังนี้

- **กันโค้ดแปลกปลอม (XSS)** — ข้อมูลที่มาจากคนอื่น (ชื่อผู้ส่ง หมายเหตุ ข้อความตอบกลับ **และรูปใบเสร็จ**) ถูก escape ทุกจุด
  และรูปต้องผ่านตัวตรวจว่าเป็น data URL ของรูปจริงเท่านั้น (`ExpenseStore.safeImage`) ทั้งตอนรับจากเซิร์ฟเวอร์ ตอนบันทึก
  และตอนกู้คืนไฟล์สำรอง · เดิมรูปที่แนบมากับใบแจ้งหนี้สามารถแทรกโค้ดเข้ามาได้ ซึ่งอันตรายมากเพราะโทเคนล็อกอินอยู่ใน localStorage
- **กันปลอมตัวตนในฐานข้อมูล** — อีเมลผู้ส่งใบแจ้งหนี้ (`from_email`), อีเมลเจ้าของรายชื่อเพื่อน (`owner_email`)
  และอีเมลของเครื่องที่รับ push ถูกบังคับให้มาจาก **โทเคนที่ล็อกอินจริง** (`auth.jwt()`) ไม่ใช่ค่าที่ฝั่งผู้ใช้ส่งมา
  → ปลอมว่าเป็นคนอื่นส่งบิล, แอบเป็นคนที่เพิ่มเพื่อน, หรือลงทะเบียนดักรับแจ้งเตือนของคนอื่น ไม่ได้อีกต่อไป
- **ป้ายเตือนคนแปลกหน้า** — ใบแจ้งหนี้จากอีเมลที่ไม่ได้อยู่ในรายชื่อเพื่อนจะขึ้น “⚠️ ไม่ใช่เพื่อนในรายชื่อ” พร้อมอีเมลผู้ส่ง
  (ใครก็ส่งบิลมาที่อีเมลเราได้ จึงต้องดูให้ชัดก่อนโอน)
- **CSP ฝังในหน้าเว็บ + กัน clickjacking** — โฮสต์ปัจจุบัน (Render Static Site ที่สร้างจากหน้า dashboard) ไม่ได้อ่าน
  `render.yaml` จึงตั้ง header ไม่ได้ · แอปจึงใส่ `Content-Security-Policy` ไว้ใน `<meta>` ของหน้าเว็บเอง
  และมีตัวกันไม่ให้ถูกฝังใน iframe ของเว็บอื่น (จะขึ้นคำเตือนแทน) · ถ้าอยากได้ header เต็มรูปแบบ ตั้งได้ที่
  Render → Static Site → **Settings → Headers** ตามค่าที่เตรียมไว้ใน `render.yaml`
- **HTTP security headers** (`render.yaml` — ใช้ได้เมื่อ deploy ด้วย Blueprint หรือคัดลอกไปตั้งใน dashboard) — `Content-Security-Policy` (จำกัดที่มาของสคริปต์/การเชื่อมต่อ),
  `X-Frame-Options: DENY` + `frame-ancestors 'none'` (กันถูกฝังใน iframe หลอกคลิก), `Permissions-Policy`
  (เปิดกล้องเฉพาะโดเมนนี้ ปิดไมค์/พิกัด/การจ่ายเงิน), `Cross-Origin-Opener-Policy`, `X-Content-Type-Options`, `Referrer-Policy`
  · ทดสอบแล้วว่า OCR (Tesseract worker + wasm) และการซิงก์ยังทำงานครบภายใต้ CSP
- **สิ่งที่ตรวจแล้วปลอดภัยอยู่แล้ว** — RLS แยกข้อมูลรายบัญชี (ทดสอบกับ PostgreSQL จริงทั้งสองฝั่ง), trigger กันลูกหนี้แก้ยอดเงิน,
  คีย์เข้ารหัสอยู่ใน Vault เรียกจากภายนอกไม่ได้, anon key เป็นคีย์สาธารณะโดยออกแบบ, service worker แคชเฉพาะไฟล์โดเมนตัวเอง,
  QR พร้อมเพย์รับเฉพาะตัวเลข, ข้อมูลรายจ่ายและรูปไม่เคยถูกส่งออกไปที่อื่นนอกจาก Supabase ของคุณเอง

## ใช้ง่ายขึ้น (v52)

- **ลบแล้วเลิกทำได้** — กดลบไม่ต้องยืนยัน ระบบลบทันทีแล้วขึ้นแถบ “↩️ เลิกทำ” ให้ ~7 วินาที (คืนรูปใบเสร็จกลับมาด้วย)
- **ปุ่มลัดร้านประจำ** — ร้านที่บันทึกซ้ำตั้งแต่ 2 ครั้งใน 120 วันจะขึ้นเป็นชิปในแท็บเพิ่ม แตะแล้วได้การ์ดใส่ชื่อร้าน/หมวด/ยอดล่าสุดให้ เหลือแค่ตรวจแล้วบันทึก
- **เปิดใช้งานได้ตอนเน็ตหลุด** — service worker แคชไฟล์แอปไว้ (network-first: ออนไลน์ได้ของใหม่เสมอ) บันทึกรายจ่ายออฟไลน์ได้ตามปกติ แล้วซิงก์ให้อัตโนมัติเมื่อเน็ตกลับมา
- **เมนูลัดที่ไอคอนแอป** — กดค้างไอคอน (Android / เดสก์ท็อป) เลือก “เพิ่ม” / “ลูกหนี้” / “สแกน QR” เข้าหน้านั้นได้เลย

## ความสามารถ

- **อ่านใบเสร็จอัตโนมัติ** — ลาก-วางรูป, เลือกไฟล์, ถ่ายรูปจากมือถือ, หรือวาง (Ctrl+V) ได้ทีละหลายใบ
  ระบบใช้ [Tesseract.js](https://tesseract.projectnaptha.com/) อ่านข้อความภาษาไทย–อังกฤษในเครื่อง
- **ให้ iPhone ดึงใบเสร็จจากอัลบั้มมาให้เอง** — เว็บแอปเปิดอัลบั้มรูปเองไม่ได้ (เบราว์เซอร์ห้ามไว้)
  แต่แอป Shortcuts ทำได้ จึงรับข้อความใบเสร็จผ่าน `#slip=<ข้อความ>` ได้
  ให้คำสั่งลัด ค้นหารูปภาพ (Find Photos) → แยกข้อความออกจากรูปภาพ (Extract Text from Image) → เปิด URL มาที่แอป
  แล้วแอปจะแยกวันที่/ยอด/ร้านให้เป็นการ์ดรอบันทึกทันที · ส่งหลายใบพร้อมกันได้โดยคั่นด้วย `###SLIP###`
  (ไม่ใช้เส้นขีดเป็นตัวคั่น เพราะสลิปหลายแบบมีเส้นขีดคั่นอยู่ข้างในอยู่แล้ว)
  · ระบุสมุดปลายทางได้ด้วย `#book=<ชื่อสมุด>&slip=<ข้อความ>` (ถ้าไม่ระบุจะเข้าสมุดที่กำลังใช้อยู่)
  · ใช้ `#` ไม่ใช่ `?` ข้อความจึง **ไม่ถูกส่งไปที่เซิร์ฟเวอร์** · วิธีตั้งค่าอยู่ในแอปแล้ว (แท็บเพิ่มรายจ่าย)
  พร้อมช่องเลือกสมุดและปุ่มคัดลอกลิงก์ที่ประกอบให้เสร็จ
- **ดึงข้อมูลให้เอง** — วันที่ (รองรับ พ.ศ. เช่น `14/02/2567`, `1 ธ.ค. 2567`, `15 Jan 2025`),
  ยอดที่ต้องจ่าย (ให้น้ำหนักบรรทัด "รวมทั้งสิ้น / ยอดสุทธิ / ยอดรวม / Total" มากกว่า "เงินสด"
  และข้ามบรรทัด VAT / เงินทอน / ส่วนลด), ชื่อร้าน, และเดารายการย่อยกับหมวดค่าใช้จ่าย
- **รองรับสลิปโอนเงินจากแอปธนาคาร** — จับ "ร้าน/ผู้ขาย" จากปลายทางที่โอนไป (โอนไปยัง / ผู้รับเงิน / ร้านค้า)
  ไม่ใช่หัวสลิป, ดึงยอดจาก "จำนวนเงิน" โดยข้ามค่าธรรมเนียมและรหัสอ้างอิง, และดึงช่อง "บันทึกช่วยจำ" ของสลิปมาให้
  รวมถึงสลิป **จ่ายบิล** ที่ผู้รับเป็นชื่ออังกฤษอย่าง `THAILANDPOST QR PAYMENT` ก็จับได้ ไม่ไปเอาชื่อผู้จ่าย
- **วันที่** ยึดบรรทัดที่มีป้ายกำกับ (วันที่ทำรายการ / วันที่โอน / วันที่) ก่อนเลขอื่นที่หน้าตาคล้ายวันที่
  รองรับปี พ.ศ. สองหลักด้วย (`4 ก.ย. 69` → 2026-09-04)
- **ทนการอ่านเพี้ยนของ OCR** — เทียบคำที่โครงพยัญชนะและยอมให้ต่างกันได้ 1 ตัว
  จึงยังจับคำได้แม้ OCR อ่านเป็น `ขำนวนเงน` (จำนวนเงิน) หรือ `ปันทึกชวยจำ` (บันทึกช่วยจำ)
  ส่วนคำสั้นๆ (โครงพยัญชนะไม่ถึง 4 ตัว) ต้องตรงตัวเต็ม ไม่งั้นป้าย `ถึง` จะไปตรงกับชื่อร้าน `ถุงเงิน`
- **ชื่อเดือนไทยที่อ่านเพี้ยน** — `กุย` `ก,ย,` `ก:ย:` `กันย.` ยังรู้ว่าเป็น ก.ย. ทั้งหมด
  โดยเทียบที่โครงพยัญชนะและชื่อเต็มที่ถูกตัดท้าย แต่ยังแยก ม.ค. กับ มี.ค. ออกจากกันได้
- **อ่านสองรอบเมื่อจำเป็น** — รอบแรกใช้ PSM 6 ซึ่งอ่านสลิปแบบสองคอลัมน์ได้ดีที่สุด
  ถ้าได้ข้อมูลไม่ครบจะอ่านซ้ำด้วย PSM 4 แล้วรวมผล เพราะบางสลิปวางยอดเงินเป็นตัวเลข
  ก้อนใหญ่กลางหน้า ซึ่งโหมดแรกจะข้ามไป
- **แก้ไขก่อนบันทึกได้เสมอ** — OCR ไม่มีวันแม่น 100% ทุกช่องจึงแก้ได้ พร้อมกดดูข้อความดิบที่อ่านได้
- **กดรูปใบเสร็จเพื่อขยายดู** — ทั้งใบที่รออ่านและรายการที่บันทึกไว้แล้ว ใบที่ยังอยู่ในคิวจะขยาย
  ด้วยไฟล์ต้นฉบับความละเอียดเต็ม จึงซูมอ่านตัวเลขเทียบกับที่ระบบอ่านได้
- **อ่านไม่ผ่านก็ไปต่อได้** — ถ้ารอบแรกล้ม ระบบจะลองใหม่อัตโนมัติด้วยรูปที่เล็กลง (กันเครื่องหน่วยความจำน้อย)
  ถ้ายังไม่ได้จะบอกสาเหตุที่เข้าใจได้พร้อมรายละเอียด และมีปุ่ม “ลองอ่านใหม่” กับ “กรอกเอง” ให้เสมอ
- **สรุปรายจ่าย** — ยอดรวมรายเดือน, เทียบกับเดือนก่อน, จำนวนรายการ, เฉลี่ยต่อรายการ,
  กราฟแยกตามหมวด, แนวโน้ม 6 เดือนล่าสุด และร้านที่จ่ายมากที่สุด
- **สรุปรายจ่ายรายวัน** — แท่งกราฟทุกวันของเดือนที่เลือก วันนี้ถูกเน้นสี วันที่ยังมาไม่ถึงจางลง
  พร้อมยอดรวม, ค่าเฉลี่ยต่อวัน (หารด้วยวันที่ผ่านมาแล้วจริง ไม่ใช่ทั้งเดือน), ยอดของวันนี้,
  จำนวนวันที่มีรายจ่าย และอันดับ 3 วันที่จ่ายมากที่สุด
  · **กรองเฉพาะหมวดที่สนใจได้** เช่น ดูว่าค่าอาหารตกวันละเท่าไหร่ โดยไม่กระทบตัวเลขสรุปส่วนอื่น
- **ลูกหนี้ — ออกให้เพื่อนก่อนแล้วหารกัน** ตอนบันทึกรายจ่าย เปิด “➗ หารกับเพื่อน / ออกให้ก่อน”
  ใส่จำนวนคนแล้วกด “แบ่งเท่ากัน” หรือใส่ยอดของแต่ละคนเอง · แท็บ **ลูกหนี้** รวมยอดค้างรายคน
  กด “รับเงินแล้ว” ทีละรายการหรือ “รับครบแล้ว” ทั้งคนก็ได้ · มีป้ายยอดค้างบนแท็บและบนการ์ดรายการ
  ยอดรายจ่ายยังนับเต็มจำนวนที่จ่ายจริง ส่วนหน้าสรุปจะบอกว่าออกให้เพื่อนไปเท่าไหร่และหักแล้วเหลือเท่าไหร่
- **งบประมาณรายเดือน + เตือนเมื่อใช้เกิน** — ตั้งงบรวมต่อเดือน และตั้งงบรายหมวดเพิ่มได้
  หน้าสรุปจะแสดงแถบความคืบหน้า ยอดคงเหลือ และค่าเฉลี่ยที่ใช้ได้ต่อวันในวันที่เหลือของเดือน
  เมื่อใช้ถึง 80% จะขึ้นเตือน "ใกล้เต็มงบ" และเมื่อเกินงบจะขึ้นเตือน "ใช้เกินงบ" ทั้งตอนบันทึกรายจ่าย
  และเป็นแบนเนอร์ค้างไว้บนหน้าแรก · หมวดที่ใช้เกินงบจะถูกทำเครื่องหมายในกราฟแยกหมวด
- **หมวดค่าใช้จ่าย 14 หมวด** — อาหาร · ของใช้/ซูเปอร์ · เดินทาง · ค่าน้ำ/ค่าไฟ · ค่าห้องพัก/คอนโด ·
  ค่าผ่อนรถยนต์ · ค่าบัตรเครดิต · บิล & ค่าบริการ · สุขภาพ · ช้อปปิ้ง · ค่าหวย/ล็อตเตอรี่ ·
  ครอบครัว · เงินยืม · บันเทิง
- **เดาหมวดให้เอง** — จากชื่อร้านและบันทึกช่วยจำ โดยให้น้ำหนักสองส่วนนี้มากกว่าข้อความอื่นในใบเสร็จ
  และคำที่เจาะจงกว่าชนะคำกว้าง ๆ (`ค่าน้ำมัน` ชนะ `น้ำ`) ที่สำคัญคือ **จำหมวดที่คุณเคยเลือกให้ร้านนั้น**
  ครั้งต่อไปที่เจอร้านเดิมจะใส่หมวดเดิมให้เลย ถ้าเลือกหมวดเองแล้วระบบจะไม่เปลี่ยนทับ
- **กันบันทึกซ้ำ** — ถ้าสแกนสลิปใบเดิมซ้ำ (เลขที่รายการเดียวกัน) หรือมีรายการวันเดียวกัน
  ยอดเท่ากัน ร้านเดียวกันอยู่แล้ว จะเตือนก่อนบันทึก และปุ่ม "บันทึกทั้งหมด" จะข้ามใบที่ซ้ำให้เอง
- **มีได้หลายสมุด** — แยกเป็นสมุดส่วนตัว / สมุดร้าน / สมุดบ้าน ได้ตามใจ สลับสมุดจากแถบด้านบน
  แต่ละสมุดมีรายการ งบประมาณ และสรุปของตัวเองแยกกันหมด เพิ่ม / เปลี่ยนชื่อ / ลบสมุดได้ในปุ่ม "จัดการสมุด"
  (ลบสมุดแล้วรายการในสมุดนั้นจะถูกลบไปด้วย และต้องเหลืออย่างน้อย 1 สมุดเสมอ)
- **แท็บเพื่อน 👥 + บัตรของฉันพร้อม QR** — อีเมลที่ล็อกอินคือรหัสประจำตัวของคุณ แท็บเพื่อนมีการ์ดทางเข้า 2 ใบ
  (“🪪 บัตรของฉัน” และ “➕ เพิ่มเพื่อน”) เปิดเป็นหน้าต่างเมื่อแตะ ด้านล่างเป็นรายชื่อเพื่อน · บัตรของฉันมี
  “บัตรของฉัน” แสดง QR ให้เพื่อนใช้กล้องมือถือสแกนแล้วเปิดลิงก์ `#addfriend=อีเมล&name=ชื่อเล่น`
  แอปจะถามยืนยันแล้วเพิ่มคุณเข้ารายชื่อเพื่อนของเขาให้ทันที (ตั้งชื่อเล่นที่เพื่อนจะเห็นได้ในบัตร)
  ฝั่งรับกด **“📷 สแกน QR ด้วยกล้อง”** ในหน้าต่างเพิ่มเพื่อน (สแกนจากในแอป ใช้ `BarcodeDetector` ของเบราว์เซอร์ ถ้าไม่มีจะโหลด
  `assets/vendor/jsQR.js` มาอ่านภาพจากกล้องเอง) · หรือกด “📤 แชร์ลิงก์เพิ่มเพื่อน” ส่งทางแชต / “📋 วางลิงก์” / พิมพ์อีเมล
  · ถ้าสแกนด้วยกล้องของ iPhone ลิงก์จะเปิดใน Safari ซึ่งเป็นคนละที่เก็บข้อมูลกับแอปบนหน้าจอโฮม แอปจะเพิ่มให้ใน Safari
  พร้อมปุ่มคัดลอกลิงก์ไปวางในแอป — แนะนำให้สแกนจากในแอปแทน
  รายชื่อเพื่อนบอกยอดค้างระหว่างกันทั้งสองทาง (QR สร้างในเครื่องด้วย `assets/vendor/qrcode.js` ไม่ส่งข้อมูลออกไปไหน)
- **เพื่อนแบบสองทาง** — เพิ่มเพื่อนฝั่งเดียวพอ พอเพื่อนซิงก์ครั้งถัดไป เขาจะเห็นคุณในรายชื่อของเขาเองด้วย
  (พร้อมชื่อเล่นจากบัตรของคุณ) ถ้าเขาลบคุณออก ระบบจะไม่เพิ่มกลับให้ซ้ำ · ต้องรัน `schema.sql` เวอร์ชัน 41 ขึ้นไป
- **ตัวเลขบนแท็บ** — แท็บลูกหนี้โชว์จำนวนรายการที่ยังค้างชำระ (ทั้งเพื่อนค้างเรา และเราค้างเพื่อน · แตะค้างเพื่อดูยอดเงิน)
  แท็บเพื่อนโชว์จำนวนเพื่อนในรายชื่อ
- **กระดิ่งแจ้งเตือน 🔔** (มุมขวาบน) — ขึ้นตัวเลขเมื่อเพื่อนส่งยอดหนี้มา แจ้งว่าจ่ายแล้ว หรือยืนยันรับเงิน
  แอปเช็กให้เองทุก 2 นาทีขณะเปิดอยู่และทันทีที่กลับมาเปิด · แตะรายการเพื่อไปแท็บลูกหนี้ · เปิดดูแล้วถือว่าอ่านแล้ว
  จำนวนที่ยังไม่อ่านโชว์บน **ชื่อแท็บเบราว์เซอร์** เช่น “(2) สมุดเก็บรายจ่ายของฉัน” และบน **ไอคอนแอปที่ติดตั้งไว้**
  (`setAppBadge` — Chrome/Edge บนเดสก์ท็อป, Android, iOS 16.4+)
- **แจ้งเตือนตอนปิดแอป (Web Push)** — ในกระดิ่งกด “🔔 เปิดแจ้งเตือนตอนปิดแอป” เครื่องนั้นจะได้รับแจ้งเตือน
  แม้ไม่ได้เปิดแอป เมื่อเพื่อนส่งยอดมา แจ้งจ่ายแล้ว ยืนยันรับเงิน หรือแก้ยอด · แตะแจ้งเตือนแล้วเปิดแอปที่กระดิ่ง
  ต้องตั้งค่าฝั่ง Supabase ก่อนหนึ่งครั้ง (ดูหัวข้อ [แจ้งเตือนตอนปิดแอป](#แจ้งเตือนตอนปิดแอป-web-push)) ·
  บน iPhone ต้องเพิ่มแอปไว้ที่หน้าจอโฮมแล้วเปิดจากไอคอนนั้น (iOS 16.4+)
- **หารบิล: เลือกชื่อจากรายชื่อเพื่อนได้** — ช่อง “👥 เลือกเพื่อน” ในแต่ละแถว หรือพิมพ์ชื่อเองก็ได้เหมือนเดิม
  (พิมพ์ชื่อตรงกับเพื่อนในรายชื่อพอดี ระบบจับคู่ให้เอง) คนที่เลือกจากรายชื่อจะ **ถูกส่งใบแจ้งหนี้ให้ทันทีตอนบันทึก**
  โดยไม่ต้องกด “ส่งให้เพื่อน” — เครื่องเพื่อนจะได้กระดิ่งแจ้งเตือน · แก้ยอดทีหลังใบแจ้งหนี้ที่ยังรอจ่ายก็อัปเดตตาม
- **ส่งยอดหนี้ให้เพื่อนได้จริง** — กด “📨 ส่งให้เพื่อน” ในแท็บลูกหนี้ แล้ว **เลือกช่องทาง**: ในแอป AmPayPay
  (เพื่อนเห็นในกระดิ่ง กดจ่ายแล้ว/สแกน QR ได้) · ทางอีเมล (เปิดแอปอีเมลพร้อมข้อความ ยอด ร้าน เลขพร้อมเพย์ และลิงก์เพิ่มเพื่อน
  — ถ้าล็อกอินอยู่จะส่งในแอปควบคู่ให้ด้วยเพื่อติดตามสถานะ) · แชร์ไปแอปอื่นเช่น LINE (บนมือถือ)
  เพื่อนที่ใช้เว็บนี้และล็อกอินด้วยอีเมลนั้นจะเห็นหัวข้อ **“หนี้ที่ฉันต้องจ่าย”** ในเครื่องของเขาเอง
  กด “จ่ายแล้ว” พร้อมข้อความตอบกลับได้ · ฝั่งเราเห็นสถานะและกด “ยืนยันได้รับ” เพื่อตัดยอดค้างในรายการจริง
  ฝั่งลูกหนี้กด **“✅ จ่ายแล้ว + บันทึกรายจ่าย”** ปุ่มเดียว = บันทึกส่วนที่ต้องจ่ายลงสมุดของตัวเอง (พร้อมรูปใบเสร็จ นับในรายการ/สรุป/งบ
  ใช้ชื่อร้านและวันที่จากใบแจ้งหนี้ เดาหมวดให้ กันบันทึกซ้ำ) และแจ้งเพื่อนว่าจ่ายแล้วพร้อมข้อความ · ปุ่มเดียวกันอยู่ในหน้าต่าง QR โอนคืน ·
  ใบที่เคลียร์แล้วยังมี “📥 บันทึกเป็นรายจ่าย” แยกไว้
  · ใบแจ้งหนี้แนบ **รูปย่อใบเสร็จ** ของเจ้าหนี้ไปด้วย (คอลัมน์ `debt_claims.image`, data URL ≤150KB) ลูกหนี้กดขยายดูได้
  และเมื่อบันทึกเป็นรายจ่าย รูปจะติดไปกับรายการ
- **QR พร้อมเพย์แนบไปกับยอดหนี้** — ใส่เบอร์โทร/เลขบัตรพร้อมเพย์ในบัตรของฉัน (แท็บเพื่อน) แล้วทุกยอดที่ส่งให้เพื่อน
  จะมีปุ่ม **“💳 QR โอนคืน”** ฝั่งเพื่อน เปิดแล้วได้ QR พร้อมเพย์ที่ใส่ยอดเงินให้แล้ว (มาตรฐาน Thai QR / EMVCo สร้างในเครื่อง
  ด้วย `assets/promptpay.js`) กด “บันทึกรูป QR” แล้วสแกนจากรูปในแอปธนาคาร หรือคัดลอกเลขพร้อมเพย์ไปโอนเอง ·
  ต้องรัน `schema.sql` เวอร์ชัน 46 ขึ้นไป (คอลัมน์ `debt_claims.promptpay`) ถ้ายังไม่รัน ยอดยังส่งได้แต่ไม่มี QR
  ยอดค้างทั้งสองทาง (เพื่อนค้างเรา / เราค้างเพื่อน) แสดงเป็นป้ายบนแท็บ
- **จัดการรายการ** — ค้นหา / กรองตามหมวดและเดือน / แก้ไข / ลบ
- **ส่งออกข้อมูล** — ดาวน์โหลด CSV (เปิดใน Excel ได้ ภาษาไทยไม่เพี้ยน) และสำรอง/กู้คืนเป็นไฟล์ JSON
- **ล้างข้อมูลได้เมื่อต้องการเริ่มใหม่** — ปุ่ม “🧹 ล้างข้อมูลในเครื่อง” ในแท็บรายการ (ถามซ้ำสองครั้ง
  และเลือกได้ว่าจะล้างธีม/รูปพื้นหลังกับออกจากระบบด้วยไหม) · ฝั่งเซิร์ฟเวอร์ใช้
  [`supabase/reset.sql`](supabase/reset.sql) ซึ่งมีให้เลือกทั้งลบเฉพาะข้อมูล ลบเฉพาะบัญชีเดียว
  รื้อตารางสร้างใหม่ และลบบัญชีผู้ใช้
- **แต่งพื้นหลังเองได้** — กดปุ่ม 🎨 เลือกรูปจากเครื่องมาเป็นพื้นหลัง หรือเลือกชุดสีสำเร็จรูป
  (มินต์ · พระอาทิตย์ · ท้องฟ้า · ชาเขียว) ปรับได้สามอย่าง: ความจางของพื้นหลัง, ความเบลอ
  และ **ความทึบของการ์ด (20–100%)** — ลดความทึบแล้วการ์ดจะกลายเป็นกระจกฝ้า มองเห็นรูปทะลุขึ้นมาทั้งหน้า
  และ **ฝ้าหลังการ์ด (0–40px)** — ตั้ง 0 คือเห็นรูปชัดทะลุการ์ดไปเลย ตั้งสูงคือกระจกฝ้าแบบเบลอ
  เมื่อการ์ดโปร่งกว่า 70% ระบบจะเพิ่มเงาใต้ตัวอักษรและยกสีตัวหนังสือที่จางขึ้นมาหนึ่งขั้นให้เอง
  ระหว่างปรับ กล่องตั้งค่าจะไม่บังหน้าจอ จึงเห็นผลทันที · รูปถูกย่อและเก็บไว้ในเครื่องนี้เท่านั้น
  โดยเก็บใน **IndexedDB** ไม่ใช่ `localStorage` เพราะ Safari บน iPhone ให้พื้นที่ `localStorage`
  แค่ราว 5 MB และนับเป็น UTF-16 (2 ไบต์ต่อตัวอักษร) รูปพื้นหลังจึงใส่ไม่ลงบน iOS ทั้งที่ลงได้บน Android
  ถ้าเครื่องไหนใช้ IndexedDB ไม่ได้ ระบบจะย่อรูปลงเรื่อยๆ จนเขียนลงที่เก็บสำรองได้จริง
- **ไอคอนประจำหมวด** — ทุกหมวดมีไอคอนของตัวเอง เห็นในช่องเลือกหมวด รายการ กราฟ และการตั้งงบ
- **เพิ่มลงหน้าจอโฮมได้เหมือนแอป** — มีไอคอนของตัวเอง (`apple-touch-icon` แบบ PNG สำหรับ iOS
  ซึ่งไม่รองรับไอคอน SVG) และ `manifest.webmanifest` สำหรับ Android/Chrome พร้อมไอคอนแบบ maskable
  เปิดแล้วแสดงเต็มจอไม่มีแถบเบราว์เซอร์ ชื่อบนหน้าจอโฮมคือ “สมุดรายจ่าย”
- ใช้ได้ทั้งบนมือถือและคอม รองรับธีมสว่าง/มืด และเคารพการตั้งค่า “ลดการเคลื่อนไหว” ของเครื่อง

## ซิงก์ข้ามเครื่องด้วย Supabase (ไม่บังคับ)

เปิดใช้แล้วรายจ่ายและงบประมาณจะซิงก์ระหว่างมือถือกับคอมให้อัตโนมัติ โดย **รูปใบเสร็จไม่ถูกอัปโหลด**
ยังเก็บอยู่ในเบราว์เซอร์ของแต่ละเครื่องเท่านั้น ถ้าไม่ตั้งค่า แอปจะทำงานแบบเก็บในเครื่องล้วนเหมือนเดิม

ตั้งค่าสามขั้น (ทำครั้งเดียว):

1. **สร้างตาราง** — เปิด Supabase → SQL Editor → วางไฟล์ [`supabase/schema.sql`](supabase/schema.sql) ทั้งไฟล์ → Run
   (สร้างตาราง `books` / `expenses` / `budgets` / `friends` / `debt_claims`
   พร้อมเปิด Row Level Security ให้แต่ละคนเห็นเฉพาะข้อมูลตัวเอง — ยกเว้นใบแจ้งหนี้ที่ลูกหนี้เห็นเฉพาะใบที่ส่งถึงอีเมลตัวเอง
   และมี trigger กันไม่ให้ลูกหนี้แก้ยอดเงิน แก้ได้แค่สถานะกับข้อความตอบกลับ)

   > **ถ้าเคยรันไฟล์นี้ไปแล้ว ให้รันซ้ำอีกครั้ง** ไฟล์นี้รันซ้ำได้ไม่พัง —
   > เวอร์ชัน 20 เพิ่มตาราง `books` กับคอลัมน์ `book_id` (จำเป็นสำหรับหลายสมุด),
   > เวอร์ชัน 31 เพิ่มคอลัมน์ `split` ให้ `expenses` (จำเป็นสำหรับข้อมูลลูกหนี้)
   > เวอร์ชัน 38 เพิ่มตาราง `friends` กับ `debt_claims` (จำเป็นสำหรับการส่งยอดหนี้ให้เพื่อน)
   > เวอร์ชัน 41 เพิ่มคอลัมน์ `owner_email`/`owner_name` กับสิทธิ์อ่านให้ `friends` (จำเป็นสำหรับเพื่อนแบบสองทาง)
   > เวอร์ชัน 43 เพิ่มตาราง `push_subscriptions` (แจ้งเตือนตอนปิดแอป) · เวอร์ชัน 46 เพิ่มคอลัมน์ `debt_claims.promptpay` (QR โอนคืน)
   > · เวอร์ชัน 48 เพิ่มคอลัมน์ `debt_claims.image` (รูปใบเสร็จในใบแจ้งหนี้)
   > · เวอร์ชัน 51 ย้ายอีเมล/พร้อมเพย์ไปเก็บแบบเข้ารหัส (ตาราง `*_data` + view ชื่อเดิม) — ดูหัวข้อ “ข้อมูลส่วนตัวในฐานข้อมูลถูกเข้ารหัส”
   > ถ้ายังไม่รัน แอปยังซิงก์รายจ่ายได้ตามปกติ แค่ระบบเพื่อนจะยังใช้ไม่ได้และจะมีข้อความบอกไว้

   > **ข้อมูลแยกตามบัญชีเสมอ** — ถ้าเครื่องเดียวกันสลับไปล็อกอินอีกบัญชี แอปจะหยุดซิงก์แล้วถามก่อน
   > ว่าจะ *ล้างข้อมูลในเครื่องแล้วดึงของบัญชีใหม่* หรือ *ย้ายข้อมูลในเครื่องเข้าบัญชีใหม่*
   > จึงไม่มีทางที่ข้อมูลของสองบัญชีจะปนกัน
2. **ตั้งค่าลิงก์กลับ** — Authentication → URL Configuration → ใส่ **Site URL** เป็น URL ของเว็บคุณ
   เช่น `https://ampaypay.onrender.com` เพื่อให้ลิงก์ยืนยันในอีเมลพากลับมาที่แอป
3. **ใส่คีย์** — คัดลอก Project URL และ publishable key (anon) จาก Settings → API มาใส่ใน
   [`assets/config.js`](assets/config.js)

### ข้อมูลส่วนตัวในฐานข้อมูลถูกเข้ารหัส (v51+)

อีเมลเพื่อน อีเมลในใบแจ้งหนี้ พร้อมเพย์ และอีเมลของเครื่องที่รับ push **เก็บแบบเข้ารหัส** ในตาราง `friends_data`,
`debt_claims_data`, `push_subscriptions_data` (pgcrypto `pgp_sym_encrypt` คีย์อยู่ใน **Supabase Vault** ชื่อ `ampaypay_pii_key`
สร้างให้อัตโนมัติตอนรัน `schema.sql`) พร้อมคอลัมน์ `…_hash` = sha256(lower(email)) ไว้ค้นหา/กำหนดสิทธิ์โดยไม่ต้องถอดรหัส

- ใน Table Editor จะเห็นอีเมล/พร้อมเพย์เป็นรหัส (`bytea`) เท่านั้น
- แอปอ่าน-เขียนผ่าน **view ชื่อเดิม** (`friends`, `debt_claims`, `push_subscriptions`) ซึ่งถอดรหัสให้เฉพาะแถวที่ RLS อนุญาต
  (`security_invoker`) และมี INSTEAD OF trigger แปลงการเขียนให้ (insert = upsert)
- รัน `schema.sql` ซ้ำครั้งเดียว: ตารางเก่าที่เก็บอีเมลเป็นข้อความจะถูก **ย้ายเป็นแบบเข้ารหัสให้อัตโนมัติ** (ลบคอลัมน์ข้อความเดิม)
  และ trigger webhook push จะถูกปรับให้ส่งข้อมูลที่ถอดรหัสแล้วเฉพาะช่องที่จำเป็น (ไม่ส่งรูป/พร้อมเพย์) ให้ Edge Function
- อีเมลที่ใช้ล็อกอิน (Authentication → Users) เป็นของระบบ Supabase เอง ไม่อยู่ในขอบเขตนี้
- ข้อจำกัด: แฮช sha256 ของอีเมลไม่ใช่การเข้ารหัส (คนที่รู้อีเมลอยู่แล้วคำนวณแฮชเทียบได้) ใช้เพื่อค้นหาเท่านั้น ·
  `pii_key()` เรียกได้เฉพาะ postgres/service role · แอปรุ่นนี้ยังใช้กับตารางแบบเก่าได้ (ตรวจโครงอัตโนมัติ)

### แจ้งเตือนตอนปิดแอป (Web Push)

ทำครั้งเดียวจากหน้าเว็บ Supabase ไม่ต้องติดตั้งอะไรในเครื่อง:

1. **รัน `supabase/schema.sql` ซ้ำ** (SQL Editor) — เพิ่มตาราง `push_subscriptions`
2. **สร้าง Edge Function** — Edge Functions → *Deploy a new function* → *Via Editor* → ตั้งชื่อ `push-notify`
   - วางเนื้อหา [`supabase/functions/push-notify/index.ts`](supabase/functions/push-notify/index.ts) ลงในไฟล์ `index.ts`
   - เพิ่มไฟล์ที่สอง `webpush.ts` แล้ววางเนื้อหา [`supabase/functions/push-notify/webpush.ts`](supabase/functions/push-notify/webpush.ts)
   - ปิด **Verify JWT** ของฟังก์ชันนี้ (เราใช้รหัสลับ `x-webhook-secret` แทน) แล้วกด Deploy
3. **ใส่ Secrets** — Edge Functions → Secrets:

   | ชื่อ | ค่า |
   |------|-----|
   | `VAPID_PUBLIC_KEY` | คีย์สาธารณะ ตัวเดียวกับ `vapidPublicKey` ใน `assets/config.js` |
   | `VAPID_PRIVATE_KEY` | คีย์ลับคู่กัน (ห้ามใส่ในเว็บ) |
   | `VAPID_SUBJECT` | `mailto:อีเมลของคุณ` |
   | `WEBHOOK_SECRET` | รหัสลับที่ตั้งเอง (ยาวๆ สุ่มๆ) |

4. **สร้าง Database Webhook** — ทำได้ 2 ทาง (เลือกทางเดียว)
   - หน้าเว็บ: Database → Webhooks → *Create a new hook* · Table `debt_claims` · Events **Insert** และ **Update** ·
     Type *Supabase Edge Functions* → `push-notify` · HTTP Headers เพิ่ม `x-webhook-secret` = ค่าเดียวกับ `WEBHOOK_SECRET`
   - หรือ SQL: แก้ `<PROJECT_REF>` กับ `<WEBHOOK_SECRET>` ใน [`supabase/push-webhook.sql`](supabase/push-webhook.sql) แล้วรันใน SQL Editor
     (สร้าง trigger ที่ยิงผ่าน `pg_net` — ผลเหมือนกัน)

   > ทำผ่าน Management API ก็ได้ (ต้องมี Access Token): `POST /v1/projects/<ref>/functions/deploy?slug=push-notify`
   > (multipart: `metadata` `{entrypoint_path:"index.ts",name:"push-notify",verify_jwt:false}` + ไฟล์ทั้งสอง),
   > `POST /v1/projects/<ref>/secrets`, และรัน SQL ข้างบนผ่าน `POST /v1/projects/<ref>/database/query` ·
   > ข้อควรระวัง: `GET /secrets` คืนค่าแบบแฮช ไม่ใช่ค่าจริง จึงต้องใช้ค่าเดียวกันกับตอนตั้งใน trigger
5. ในแอป เปิดกระดิ่ง 🔔 → กด **“🩺 ตรวจการตั้งค่า”** จะบอกว่าตาราง / ฟังก์ชัน / secrets ข้อไหนยังไม่พร้อม
   แล้วกด “เปิดแจ้งเตือนตอนปิดแอป” บนเครื่องที่อยากให้เด้ง

การทำงาน: เบราว์เซอร์สมัครรับ push กับผู้ให้บริการของมันเอง แล้วแอปจด endpoint ไว้ในตาราง `push_subscriptions`
(เห็นได้เฉพาะเจ้าของแถว) · เมื่อมีการเพิ่ม/แก้ `debt_claims` เว็บฮุกเรียก `push-notify` ซึ่งเลือกผู้รับ
(ลูกหนี้ตาม `to_email` หรือเจ้าหนี้ตาม `from_user`) เข้ารหัสข้อความตาม RFC 8291 และลงชื่อ VAPID ด้วย WebCrypto ล้วน
(ไม่พึ่ง npm) แล้วส่งไปที่ endpoint ของแต่ละเครื่อง · endpoint ที่ตอบ 404/410 จะถูกลบทิ้ง · `sw.js` ที่หน้าเว็บลงทะเบียนไว้
ทำหน้าที่รับ push อย่างเดียว ไม่แคชไฟล์ จึงไม่กระทบการอัปเดตเวอร์ชัน

**สร้างคู่คีย์ VAPID ใหม่** (ถ้าอยากใช้คีย์ของตัวเอง) — รันใน Node:
```
node -e "const c=require('crypto'),k=c.generateKeyPairSync('ec',{namedCurve:'prime256v1'}),p=k.publicKey.export({format:'jwk'}),q=k.privateKey.export({format:'jwk'}),b=s=>Buffer.from(s,'base64url');console.log('public:',Buffer.concat([Buffer.from([4]),b(p.x),b(p.y)]).toString('base64url'));console.log('private:',q.d)"
```
แล้วใส่ public ใน `assets/config.js` และทั้งคู่ใน Secrets

### วิธีล็อกอิน

แอปรองรับสองแบบ เลือกใช้ได้ในกล่อง ☁️

**1. อีเมล + รหัสผ่าน (ค่าเริ่มต้น — ไม่ต้องพึ่งอีเมลเลย)**
ครั้งแรกใส่อีเมลกับรหัสผ่านที่ต้องการแล้วกด "สมัครใหม่" เครื่องอื่นใช้อีเมล/รหัสผ่านเดิมเพื่อเห็นข้อมูลชุดเดียวกัน

> ถ้าอยากให้สมัครแล้วเข้าใช้ได้ทันทีโดยไม่ต้องยืนยันอีเมล: Authentication → Providers → Email →
> ปิดสวิตช์ **Confirm email** (สำหรับสมุดรายจ่ายส่วนตัวถือว่าเพียงพอ เพราะข้อมูลถูกกันด้วย RLS อยู่แล้ว)

**2. ส่งลิงก์ทางอีเมล** — ใส่อีเมลแล้วกดลิงก์ในอีเมล ต้องตั้ง Site URL ตามข้อ 2 ด้านบนก่อน

### อยากได้รหัส 6 หลักในอีเมล

Supabase จะยอมให้แก้เทมเพลตอีเมล **ต่อเมื่อตั้งค่า SMTP ของตัวเองแล้วเท่านั้น** (เช่น Resend, Brevo, Gmail)
ถ้ายังใช้อีเมลของ Supabase หน้าเทมเพลตจะล็อกไว้ แก้ไม่ได้

ตั้ง SMTP เสร็จแล้วไปที่ Authentication → Emails → **Magic Link** แล้วใส่ `{{ .Token }}` ลงในเนื้ออีเมล เช่น

```html
<h2>เข้าสู่ระบบสมุดเก็บรายจ่าย</h2>
<p>รหัสเข้าใช้งานของคุณคือ</p>
<p style="font-size:28px;letter-spacing:6px;font-weight:bold">{{ .Token }}</p>
<p>รหัสมีอายุ 1 ชั่วโมง</p>
<p>หรือกดลิงก์นี้เพื่อเข้าใช้งานทันที: <a href="{{ .ConfirmationURL }}">เข้าสู่ระบบ</a></p>
```

แอปรองรับทั้งสองแบบ: ใส่รหัสในช่อง หรือกดลิงก์ในอีเมลแล้วกลับมาที่แอปโดยล็อกอินให้เลย

การใช้งาน: กดปุ่ม ☁️ มุมขวาบน → ใส่อีเมล → รับรหัส/ลิงก์ทางอีเมล → ล็อกอิน
หลังจากนั้นแอปจะซิงก์ให้เองทุกครั้งที่บันทึก แก้ไข หรือลบรายการ และกด "ซิงก์ตอนนี้" เองก็ได้

**ข้อควรรู้เรื่องคีย์:** publishable key (anon) ออกแบบมาให้ฝังในหน้าเว็บได้ ความปลอดภัยมาจาก
Row Level Security ในฐานข้อมูล ห้ามนำ `service_role` key มาใส่ในโค้ดฝั่งเว็บเด็ดขาด

## วิธีใช้งาน

เป็นไฟล์สแตติกล้วน ไม่ต้อง build ไม่มี dependency ที่ต้องติดตั้ง

```bash
git clone https://github.com/bmsjib5-ux/AmPayPay.git
cd AmPayPay
python3 -m http.server 8000     # หรือ npx http-server -p 8000
# เปิด http://localhost:8000
```

> แนะนำให้เปิดผ่าน http(s) ไม่ใช่ดับเบิลคลิกไฟล์ (`file://`) เพราะตัวอ่าน OCR ทำงานผ่าน Web Worker
> ถ้าจะเอาขึ้นออนไลน์ อัปโหลดทั้งโฟลเดอร์ขึ้น GitHub Pages / Netlify / Vercel ได้เลย

### ขึ้นออนไลน์ด้วย Render (ฟรี)

รีโปนี้มี [`render.yaml`](render.yaml) ให้แล้ว Render จะตั้งค่าเป็น Static Site ให้อัตโนมัติ

1. ล็อกอิน [dashboard.render.com](https://dashboard.render.com) แล้วเชื่อมบัญชี GitHub
2. กด **New +** → **Static Site** → เลือกรีโป `bmsjib5-ux/AmPayPay`
3. เลือก Branch ที่ต้องการ deploy (เช่น `main` หรือ `claude/expense-tracker-app-ctofm0`)
   - **Build Command:** เว้นว่างไว้ได้เลย
   - **Publish Directory:** `.`
4. กด **Create Static Site** รอสัก 1 นาที จะได้ URL หน้าตา `https://<ชื่อที่ตั้ง>.onrender.com`

ทุกครั้งที่ push ขึ้นแบรนช์นั้น Render จะ deploy ใหม่ให้เอง
(ทางเลือกอื่นที่ไม่ต้องสมัครอะไรเพิ่ม: เปิด GitHub Pages จาก Settings → Pages ของรีโปนี้)

ครั้งแรกที่อ่านใบเสร็จ เบราว์เซอร์จะดาวน์โหลดตัวอ่าน OCR และชุดภาษาไทย (~5 MB) จาก CDN
แล้วแคชไว้ ครั้งต่อไปเร็วขึ้นและใช้แบบออฟไลน์ได้

### ใช้แบบออฟไลน์เต็มรูปแบบ (ไม่พึ่ง CDN)

ดาวน์โหลดไฟล์ของ Tesseract มาไว้เองแล้วบอกที่อยู่ใน `index.html` ก่อนโหลด `assets/app.js`:

```html
<script>
  window.OCR_PATHS = {
    workerPath: 'vendor/worker.min.js',   // tesseract.js@5/dist/worker.min.js
    corePath:   'vendor/core/',           // tesseract.js-core@5 (ไฟล์ .wasm / .wasm.js)
    langPath:   'vendor/lang'             // tha.traineddata.gz, eng.traineddata.gz
  };
</script>
```

## โครงสร้างโค้ด

| ไฟล์ | หน้าที่ |
|------|---------|
| `index.html` | โครงหน้าเว็บ 5 แท็บ: เพิ่มรายจ่าย / สรุป / รายการ / ลูกหนี้ / เพื่อน |
| `assets/vendor/qrcode.js` | ไลบรารีสร้าง QR (qrcode-generator, MIT) ใช้ทำบัตรของฉันในแท็บเพื่อน |
| `assets/promptpay.js` | สร้าง payload QR พร้อมเพย์ (EMVCo + CRC-16) จากเบอร์โทร/เลขบัตร/e-Wallet |
| `assets/vendor/jsQR.js` | ตัวอ่าน QR จากภาพกล้อง (jsQR, Apache-2.0) โหลดเฉพาะตอนสแกนเมื่อเบราว์เซอร์ไม่มี BarcodeDetector |
| `sw.js` | service worker รับ push แจ้งเตือน (ไม่แคชไฟล์) |
| `supabase/functions/push-notify/` | Edge Function ส่ง Web Push เมื่อ `debt_claims` เปลี่ยน (`index.ts` เลือกผู้รับ · `webpush.ts` เข้ารหัส/VAPID) |
| `assets/styles.css` | ธีมและเลย์เอาต์ (รองรับโหมดมืด, จอมือถือ) |
| `assets/parser.js` | แปลงข้อความจาก OCR เป็นข้อมูลรายจ่าย — วันที่, ยอดเงิน, ชื่อร้าน, หมวด, รายการย่อย |
| `assets/store.js` | อ่าน/เขียนข้อมูลใน `localStorage` — สมุด, รายจ่าย, งบประมาณ (จัดการกรณีพื้นที่เต็มด้วยการทิ้งรูปย่อรายการเก่า) |
| `assets/app.js` | คิวอ่านใบเสร็จ, ฟอร์มตรวจแก้, กราฟสรุป, รายการ, ส่งออก/สำรองข้อมูล |

รูปแบบข้อมูลที่เก็บ 1 รายการ:

```json
{
  "id": "e1a2b3c",
  "bookId": "b_default",
  "date": "2024-02-14",
  "merchant": "ร้านกาแฟบ้านสวน",
  "amount": 280,
  "category": "food",
  "note": "กาแฟเช้า",
  "items": [{ "name": "ลาเต้ร้อน", "price": 85 }],
  "rawText": "ข้อความที่ OCR อ่านได้",
  "image": "data:image/jpeg;base64,... (รูปย่อ)",
  "createdAt": 1739500000000
}
```

## ข้อควรรู้เรื่องความแม่นของ OCR

- ถ่ายให้ใบเสร็จเต็มกรอบ แสงสม่ำเสมอ ไม่เอียง จะอ่านแม่นขึ้นมาก
- Tesseract มักซอยอักษรไทยเป็นตัวๆ (`ย อ ด ร ว ม`) — ตัวแปลงในโปรเจกต์นี้เชื่อมกลับให้อัตโนมัติ
- ถ้าหายอดไม่เจอ ระบบจะเดาจากตัวเลขทศนิยมที่มากที่สุดในครึ่งล่างของใบเสร็จ และจะขึ้นข้อความเตือนให้ตรวจสอบ
- ข้อมูลอยู่ในเบราว์เซอร์เครื่องเดียว: ล้าง Site data = ข้อมูลหาย จึงควรกด **สำรองข้อมูล** เป็นระยะ
